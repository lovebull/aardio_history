
namespace dotNet;

try{ 
	import ..dotNet.clr; 
}
catch(e){ 
	class clr{
		ctor( version ){
			return null,..string.format(".NET runtime %s cannot be loaded,%s",version:"",e:"" )
		}; 
	}
}

/**intellisense()
dotNet = dotNet支持库
end intellisense**/
