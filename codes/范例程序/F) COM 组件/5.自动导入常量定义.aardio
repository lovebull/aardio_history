//导入常量
//自动导入常量定义
import console;
import com

conn = com.CreateObject("ADODB.Connection") 
var tlb = com.GetTypeInfo(conn).GetTypeLib()
var constants = tlb.ExportConstants()

table.mix(self,constants)

//测试一下
console.log( adChar );

//直接通过com对象也可以访问枚举或常量( 不需要导入 )
console.log( conn.adChar ); 
console.pause();